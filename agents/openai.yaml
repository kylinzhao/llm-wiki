interface:
  display_name: "LLM Wiki"
  short_description: "用 fast/update/trace 等命令构建、维护、查询和审计 LLM Wiki"
  default_prompt: "用 $llm-wiki 处理这个项目。新项目可用 fast 一次性跑完，变更响应用 update，需求到代码审计用 trace；先读 BUSINESS_CONTEXT.md 和当前状态。"
