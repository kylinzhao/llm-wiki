[project]
name = "llm-wiki-project"
version = "0.1.0"
description = "File-based LLM Wiki deterministic tooling"
requires-python = ">=3.10"
dependencies = [
  "pyyaml>=6.0",
  "requests>=2.28",
  "beautifulsoup4>=4.12",
]

