engine_version: "engine-v1.0.0"

capabilities:
  - wiki.query
  - wiki.update

evidence:
  raw: true
  raw_code: true

phases:
  rss_sync: true

rss_config_path: "config/rss-feeds.yaml"

overrides: {}
